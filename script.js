const menuToggle=document.querySelector('.menu-toggle');const navlinks=document.querySelector('.navlinks');if(menuToggle&&navlinks){menuToggle.addEventListener('click',()=>{const open=navlinks.classList.toggle('open');menuToggle.setAttribute('aria-expanded',String(open));});navlinks.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{navlinks.classList.remove('open');menuToggle.setAttribute('aria-expanded','false')}));}
const obs=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');obs.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>obs.observe(el));
function sendWA(form){const data=new FormData(form);const nombre=data.get('nombre')||'';const telefono=data.get('telefono')||'';const perfil=data.get('perfil')||'';const txt=`Hola, soy ${nombre}. Mi WhatsApp es ${telefono}. Me interesa BellaVittoria. Perfil: ${perfil}. Quiero recibir disponibilidad vigente, brochure, ficha y la promoción de junio 2026.`;window.open(`https://wa.me/523333777337?text=${encodeURIComponent(txt)}`,'_blank','noopener');}
['promoForm','leadForm'].forEach(id=>{const f=document.getElementById(id);if(f){f.addEventListener('submit',e=>{e.preventDefault();sendWA(f);});}});